# FMOD project (stub)

Placeholder for the FMOD Studio project and the `fmod-gdextension` runtime integration
decided in `ARCHITECTURE.md`. This subfolder exists so the repo layout is stable from
checkpoint 1 onward; the FMOD Studio project, the dry tell channel, and the leitmotif
layer parameter are wired in checkpoint 8 (see the technical plan on this issue).

Until then, gameplay audio is silent/placeholder and does not route through FMOD.
